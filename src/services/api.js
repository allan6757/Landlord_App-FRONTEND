const API_BASE_URL = process.env.REACT_APP_API_URL || 'http://localhost:3001'

export const api = {
  get: async (endpoint) => {
    // API implementation
  },
  post: async (endpoint, data) => {
    // API implementation
  }
}

export default api